import ClientPage from './ClientPage';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "zen+ TV | Contact",
  description: "Experience the best contact with zen+ TV.",
};

export default function Page() {
  return <ClientPage />;
}
